module.exports = {
    SYSTEM_USERNAME: 'System',
    PORT: process.env.PORT || 8080,
    MAX_MESSAGE_LENGTH: process.env.MAX_MESSAGE_LENGTH || 5,
    IDLE_TIMEOUT: process.env.IDLE_TIMEOUT || 10000,
};
